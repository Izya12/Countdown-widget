# Local verification — 2026-09-17

Environment: Windows, JDK21, Gradle8.13, Pixel9Pro Android16/API36.1 AVD.

| Check | Result |
|---|---|
| Countdown/recurrence unit tests | 11 passed |
| Reminder planner unit tests | 7 passed |
| Backup codec unit tests | 9 passed |
| Connected database, migration, backup, notification and UI tests | 14 passed |
| Debug and unsigned release/R8 builds | Passed |
| Lint | Zero errors, 26 warnings |

Final debug APK SHA256: `FAF18D5BAFAA92A04BE8FFBB4CDEB75630F3FABC96BE5C391C90FEB1F0C14188`.

Verified notification publication through Android NotificationManager, one card after repeated processing, durable delivery record, cancellation after event deletion and rejection of reminders older than two hours. Verified rule deduplication, version changes only for schedule edits, conversion to all-day, schema2→3 and rules in JSON import/export. The create-event UI test now also adds a reminder.

The UI test was additionally run in Russian with an optional synthetic screenshot at `captures/reminders-ru.png`. Visual review found a clumsy zero-day label; it was changed to “В день события”. English/Russian plural labels now use Compose resource access.

No exact alarms or INTERNET permission were added. POST_NOTIFICATIONS is requested when a reminder is added; denial preserves rules.

## Round1×1 widget follow-up

- Latest connected suite: 15 tests passed, including a real AppWidgetHost rendering test at72×96dp; existing 27 unit tests are unchanged.
- Debug/release R8 builds passed; lint zero errors,30 warnings.
- Renderer alpha/white-fill checks and targetCellWidth/Height=1 metadata assertions passed. Host content loaded with the event accessibility description.
- Actual RU host capture reviewed: `captures/circle-widget-host.png`,22 days, red ring, transparent corners and white event caption. It matches the requested circular composition.
- Samsung One UI grid sizing and physical-device launcher placement have not been tested. The test uses Android16 AppWidgetHost; it does not automate One UI.
- The earlier debug checksum in this report predates the round-widget addition.

## Independent units and configuration follow-up

- Connected suite now16 tests, all passed. A second widget can use weeks while the first keeps days for the same event.
- Configuration selection survives Activity recreation and saving finishes the configuration Activity successfully.
- A real AppWidgetHost click launches MainActivity with the correct eventId.
- Past50-day rendering is captured as localized +7.1 weeks (+7,1 in Russian), with no remaining-progress ring.

Not yet verified: inexact alarm timing under Doze, reboot/force-stop recovery, permission revoke/re-enable across process death, real OEM launchers and signed release installation. Alarm dispatch code exists but the notification test invokes reconciliation directly; it does not measure OS alarm latency. No battery or exactly-once claims are made.
